#include<bits/stdc++.h>

using namespace std;
#define ull unsigned long long int

int main()
{
    ull n,a,nth,b;
    while(scanf("%llu",&n)==1){
       nth=(n+1)/2;
       a=(nth*nth);
       b=a-3;
       ull dnf=a*a;
       ull dnl=b*b;
       cout<<dnf-dnl<<endl;
    }
    return 0;
}
